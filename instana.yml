---
# This playbook deploys the CheckMK agent

- name: apply common configuration to all nodes
  
- name: install instana agent package
   hosts: all  
   yum: name=instana-agent-dynamic state=latest update_cache=yes disable_gpg_check=yes
